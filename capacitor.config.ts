import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sistemadepedidos.app',
  appName: 'Sistema de Pedidos',
  webDir: 'build'
};

export default config;
